// Exemplo 11: Eventos de teclado 1
// Podemos capturar eventos de teclado com addEventListener

document.addEventListener('keydown', function(event) {
    console.log('Tecla pressionada:', event.key);
});
