// Exemplo 15: Exercício de array #7
// Crie um array de nomes e exiba cada nome

let nomes = ['Ana', 'Bruno', 'Carlos'];
for (let i = 0; i < nomes.length; i++) {
    console.log(`Nome ${i + 1}:`, nomes[i]);
}
