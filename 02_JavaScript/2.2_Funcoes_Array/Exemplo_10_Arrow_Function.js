// Exemplo 10: Arrow Function
// Arrow functions são funções anônimas de sintaxe curta

const somar = (a, b) => a + b;
console.log('Soma:', somar(2, 3));

const saudacao = nome => `Olá, ${nome}!`;
console.log(saudacao('Lucas'));
